<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="#">
    <title>{{ title }}</title>

    <link rel="stylesheet" href="{{ '/styles/reset.css' | url }}">
    <link rel="stylesheet" href="{{ '/styles/fonts.css' | url }}">
    <link rel="stylesheet" href="{{ '/styles/main.css' | url }}">
</head>
<body>
    {% include "../_partials/_nav.njk" %}

    <main>
        {% block main %}
            {# the context that will be different on each of the pages #}
            {{ content | safe }}
        {% endblock main %}
    </main>

    {% include "../_partials/_footer.njk" %}
</body>
</html>